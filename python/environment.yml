name: github-action-builder
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pyyaml
  - requests
  - mypy  # 类型检查
  - pytest  # 测试
  - pip
  - pip:
    # GitHub API 客户端（如果需要）
    - PyGithub
    # 环境变量管理
    - python-dotenv

